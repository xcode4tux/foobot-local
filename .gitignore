# Python
__pycache__/
*.py[cod]

# Runtime files created by the service
service.log
server.log
inject
led_config.json

# Never commit cloud credentials (optional fallback only)
cloud_secret.json
.env

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
